<form method="post" action="{{ path('app_tiers_delete', {'id': tier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tier.id) }}">
    <button class="btn">Delete</button>
</form>
